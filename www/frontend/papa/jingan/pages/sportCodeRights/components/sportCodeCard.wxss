/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
page {
  min-height: 100vh;
  box-sizing: border-box;
}
.code-card {
  position: relative;
  min-height: 818rpx;
}
.code-card .header {
  padding: 0rpx 24rpx;
}
.code-card .header .header-center {
  min-width: 690rpx;
  height: 72rpx;
  opacity: 1;
  border: 3px solid #ddeaff;
  border-radius: 72rpx;
  background: linear-gradient(180deg, rgb(120, 185, 255) 0%, rgb(199, 220, 255) 100%);
}
.code-card .body {
  transform: translateY(-64rpx);
  min-width: 670rpx;
  margin: 0rpx 40rpx;
  min-height: 746rpx;
  opacity: 1;
  border-radius: 24rpx 24rpx 40rpx 40rpx;
  background: linear-gradient(180deg, rgb(24, 120, 224) 0%, rgb(33, 148, 255) 100%);
}
.code-card .body .body-card {
  margin-top: 16rpx;
  width: 100%;
  min-height: 730rpx;
  border-radius: 0rpx 0rpx 40rpx 40rpx;
  background: linear-gradient(180deg, #50abff 0%, #e9f4ff 10%, white 100%);
}
.code-card .body .body-card .title {
  text-align: center;
  font-size: 28rpx;
  font-weight: 500;
  color: rgb(48, 49, 51);
  margin-bottom: 32rpx;
  margin-top: 74rpx;
}
.code-card .body .body-card .refresh {
  font-size: 24rpx;
  font-weight: 400;
  color: #909399;
  margin-top: 34rpx;
}
.code-card .body .body-card .refresh text {
  margin-left: 16rpx;
}
.code-card .body .body-card .rule {
  min-width: 216rpx;
  margin-top: 40rpx;
  font-size: 24rpx;
  font-weight: 400;
  color: var(--hubei-primary, #409eff);
  margin-bottom: 40rpx;
}
.code-card .body .body-card .rule .separator {
  margin: 0rpx 16rpx;
}
.code-card .body .code-image .image {
  -o-object-fit: cover;
     object-fit: cover;
  display: block;
  width: 396rpx;
  height: 396rpx;
}
.qrcode {
  position: fixed;
  left: -10000px;
  top: -10000px;
}